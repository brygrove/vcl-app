# Note: Using Mysql in scenario tests will require that you drop and create the database first if you have 
#       an existing database with data in it. The reason is so you start with a clean state where once the scenario
#		completes it will drop the tables so it is possible to rerun the scenario. 

test.db.username=root
test.db.password=7633ax-11
test.db.driver=com.mysql.jdbc.Driver
test.db.url=jdbc:mysql://localhost:3306/atx?autoReconnect=true

test.hibernate.update_db_strategy=create-drop

test.hibernate.dialect=com.groveworks.dialect.MySQL5InnoDBDialect

