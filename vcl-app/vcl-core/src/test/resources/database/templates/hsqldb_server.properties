# Connect to an already running stand alone HSQL DB server

test.db.username=sa
test.db.password=
test.db.driver=org.hsqldb.jdbcDriver
test.db.url=jdbc:hsqldb:hsql://localhost/atx

test.hibernate.update_db_strategy=create

test.hibernate.dialect=com.groveworks.dialect.HSQLDialect

